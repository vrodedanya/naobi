import standard;

workflow main
target begin
{
    integer a = 5, b = 3;
    a = b / 3 + a - b * 4;
    println(a);
    a = int(input());
    b = int(input());
    println(a + b);
    println(a - b);
    println(a * b);
    println(a / b);

    a = b * (a + b);
    println(a);
}