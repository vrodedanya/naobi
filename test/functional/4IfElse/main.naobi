import standard;

workflow main
target onBegin
{
    float first = 5.0, second = 5.0;
    if (first == second)
    {
        if (true)
        {
            first = first * 5.0;
            println(first);
            first = second / first;
            println(first);
        }
        else
        {
            println("Ssss");
        }
    }
    else
    {
        if (false)
        {
            println(3);
        }
        else
        {
            println(4);
        }
    }
    println(first);
}