import standard;

workflow main
target onBegin
{
    integer first = 1120, second = 4;
    float test = 0.4;
    string str = "Hello world!";
    boolean third = false, check = third;
    println(test);
    println(str);
    println("Test");
    if (true)
    {
        println(third);
        println(first);
    }
    println(first + second);
}